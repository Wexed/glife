# krol
if $ARGS[0] = 'start':
	cla
	*clr
	set minut += 5
	'<center><img src="images/zoo/1791_640.jpg"></center>'
	'<center><<$namekrol>> rests.</center>'
	clr
	gs 'stat'
	fcolor = rgb(0, 0, 0)
	bcolor = rgb(255, 255, 255)
	lcolor = rgb(106, 90, 205)

	act 'Leave':gt 'korr'

	act 'Grant on skewers for meat':
		set krolik = 0
		set minut += 60
		set money += 200
		gt 'korr'
	end
end

--- krol ---------------------------------
